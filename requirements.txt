PyQt6>=6.4.0
Pillow>=9.0.0
pynput>=1.7.0          # optional — enables global hotkey

# OCR & Translate tool (downloaded on first use, ~300 MB model)
easyocr>=1.7.0         # optional — OCR engine
deep-translator>=1.11.0 # optional — Google Translate (needs internet)
